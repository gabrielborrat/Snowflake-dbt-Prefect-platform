# =============================================================================
# Snowflake Modern Data Stack Portfolio - Environment Variables Template
# =============================================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# NEVER commit .env to git — it contains secrets.
# =============================================================================

# --- Snowflake Connection ---
SNOWFLAKE_ACCOUNT=your_account.region.cloud    # e.g. xy12345.west-europe.azure
SNOWFLAKE_USER=your_username
SNOWFLAKE_PASSWORD=your_password
SNOWFLAKE_ROLE=INGESTION_ROLE                  # Default role for ingestion scripts
SNOWFLAKE_WAREHOUSE=INGESTION_WH               # Default warehouse for ingestion
SNOWFLAKE_DATABASE=RAW                         # Default database for ingestion

# --- Snowflake dbt Connection ---
# dbt uses its own profiles.yml, but these vars can be referenced there
DBT_SNOWFLAKE_ROLE=TRANSFORM_ROLE
DBT_SNOWFLAKE_WAREHOUSE=TRANSFORM_WH
DBT_SNOWFLAKE_DATABASE=ANALYTICS

# --- API Keys (optional — some APIs are free without keys) ---
# ALPHA_VANTAGE_API_KEY=your_key_here          # Only if using Alpha Vantage instead of yfinance

# --- Ingestion Settings ---
INGESTION_LOG_LEVEL=INFO                        # DEBUG, INFO, WARNING, ERROR
INGESTION_BATCH_SIZE=10000                      # Rows per batch for Snowflake uploads

# --- Prefect Settings ---
# PREFECT_API_URL=http://127.0.0.1:4200/api    # Local Prefect server (if running)
